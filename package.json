{
  "name": "trpc-test",
  "scripts": {
    "back:dev": "cd server-api && npm run dev",
    "front:dev": "cd frontend && npm run dev",
    "start": "npm-run-all --parallel back:dev front:dev"
  },
  "workspaces": [
    "frontend",
    "server-api"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
